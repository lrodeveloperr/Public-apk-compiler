name: Verify Native Android

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'compiler-input/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
  push:
    branches: [ main, feedback-fixes-no-build ]
    paths:
      - 'app/**'
      - 'compiler-input/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'

permissions:
  contents: read

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up current Android SDK tools
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: 15859902

      - name: Install Android 17 compile SDK
        shell: bash
        run: sdkmanager --channel=3 "platform-tools" "platforms;android-37" "build-tools;37.0.0"

      - name: Set up Gradle 9.5
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Verify resources, manifest, Room/KSP and Kotlin
        shell: bash
        run: >
          gradle
          :app:processDebugResources
          :app:processDebugMainManifest
          :app:compileDebugKotlin
          --no-daemon
          --stacktrace
